;; F and C should be consistent.
(include *standard-cl-23*)
(import std.shatree)

(defun F (A B) (+ A B))

(defconst C F)
(defconst Cprime (c "hithere" F))

(defconst FHASH (shatree F))
(defconst CHASH (shatree C))

(export F)
(export C)
(export Cprime)
(export FHASH)
(export CHASH)
