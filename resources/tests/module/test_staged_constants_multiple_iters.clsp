(include *standard-cl-23*)

(import std.shatree exposing shatree)

(defconst B (+ H 3))
(defconst FH (shatree F))
(defconst GH (shatree G))
(defun F (X) (+ X GH))
(defun G (X) (+ X FH))

(export F)
(export G)
